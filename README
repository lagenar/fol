Dependencias:
 -- ocaml
 -- make

Compilar:

./make

Ejecutar:
./main

Formato:

Símbolos de función, constantes y variables
van en minúscula

Las constantes se definen antes de la formula
ejemplo:
Const(a,b,c)

símbolos de predicados van en mayúscula

ejemplos de fórmulas

ForAll(x)(P(x))
P(x) ^ Q(x) v G(g,f(y))
Exists(y)(ForAll(x)(P(x,y) ^ ~Q(x,f(y))))
~P(x) => E(y)


